services:
  postgres:
    image: 'postgres:18-alpine'
    environment:
      POSTGRES_USER: depguard
      POSTGRES_PASSWORD: depguard
      POSTGRES_DB: depguard
    ports:
      - '5432'
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U depguard -d depguard" ]
      interval: 10s
      timeout: 5s
      retries: 5
